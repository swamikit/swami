# src/mapping — shared mapping helpers/config

Any *code* form of the Origami↔SwiftUI mapping that both the parser/tooling and the
skill need to share (e.g. spring-parameter conversion, enum tables).

**Not built yet.** The authoritative mapping *rules* live as prose in `CLAUDE.md` and
in the skill (`skill/`); this directory is only for the deterministic bits worth
factoring into code. Don't duplicate judgment here that belongs in the skill.
