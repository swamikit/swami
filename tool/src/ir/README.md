# src/ir — intermediate representation

The structured form the parser emits and the translation skill consumes.

**Not built yet.** Design it from what the first seed example actually needs
(`NEXT_STEPS.md` step 3) — keep it minimal.

Expected shape:
- **Patch graph** — nodes (patches) + dataflow edges.
- **Layer tree** — visual hierarchy, layout, styling.
- **Bindings** — how patches drive layer properties and how interactions feed state.
